## Nothing but a random college assignment for GENAI course.



completed with:
- Implemented interactive element detection
- Added logging for detected interactive elements
- done with main functionality for detecting interactive elements on a webpage using Playwright and logging the results.


Todo:
- complete the main , agent loop and the inspecter.
- will add CLI interface for user input and interaction with the agent.

June 13 2026:
(Done with the main functionality for detecting interactive elements on a webpage using Playwright and logging the results. Next steps include completing the main agent loop, implementing the inspector, and adding a CLI interface for user input and interaction with the agent.)




## New TODOs:
- Implement the main agent loop to continuously monitor the webpage for changes and update the list of interactive
- Test the interactive element detection on various webpages to ensure robustness and accuracy.
    - [tested-till-here]
- Add error handling and edge case management for scenarios where elements may not be easily detectable or when the webpage structure is complex.
- Optimize the performance of the element detection process to minimize latency and resource usage.
- Develop a user-friendly CLI interface for users to interact with the agent, allowing them to easily view detected elements, refresh the list, and perform actions on the elements if needed.